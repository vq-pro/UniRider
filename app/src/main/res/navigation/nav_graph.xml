<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
            xmlns:app="http://schemas.android.com/apk/res-auto"
            xmlns:tools="http://schemas.android.com/tools"
            android:id="@+id/nav_graph"
            app:startDestination="@id/MainFragment">

    <fragment
            android:id="@+id/MainFragment"
            android:name="quebec.virtualite.unirider.views.MainFragment"
            tools:layout="@layout/main_fragment">

        <action
                android:id="@+id/action_MainFragment_to_WheelFragment"
                app:destination="@+id/WheelFragment"
        />
    </fragment>

    <fragment
            android:id="@+id/WheelFragment"
            android:name="quebec.virtualite.unirider.views.WheelFragment"
            tools:layout="@layout/wheel_fragment">
    </fragment>
</navigation>
